<ul id="apps">
  {{#each apps}}
    <li class="{{#if ../inline}}inline{{/if}}">
      <div>
        ({{id}}) {{name}}<br>
        <a class="install" data-id="{{id}}">{{t "install"}}</a>
      </div>
    </li>
  {{/each}}
</ul>
